# demo-site

bring4U 日本形象網站 — Demo / Test Repository

## 部署架構

```
Bitbucket (source)  →  Vercel (auto deploy)  →  Public URL
```

每當 `main` 分支有新的 commit push 上來，Vercel 會自動重新建置並部署。

## 本地預覽

直接用瀏覽器打開 `index.html` 即可。
